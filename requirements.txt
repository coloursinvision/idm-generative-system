# IDM Generative System — Streamlit Cloud dependencies
# This file exists ONLY for Streamlit Cloud deployment.
# Canonical dependency spec: pyproject.toml [project.dependencies]
#
# Streamlit Cloud reads requirements.txt from the repo root.
# Keep this manually synchronised with pyproject.toml core deps + streamlit extra.

# DSP / Audio
numpy>=1.26.0,<2.0
scipy>=1.11.0
soundfile>=0.12.0
librosa>=0.10.0
numba>=0.59.0

# Backend (imported by knowledge/ modules used in Streamlit)
fastapi>=0.110.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Resilience
tenacity>=8.2.0

# Vector DB
qdrant-client>=1.8.0

# LLM
openai>=1.0.0

# UI
streamlit>=1.32.0
